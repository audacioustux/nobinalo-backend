{
  "watch": [
    "src"
  ],
  "verbose": false,
  "env": {
    "NODE_ENV": "development",
    "nodemon": true,
    "PORT": 4000
  },
  "exec": "npm run build && ./scripts/wait-port.sh $PORT && DEBUG=* npm start",
  "ext": "ts"
}