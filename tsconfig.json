{
    // tsconfig.json
    // NOTE: this configuration is only for linting and compatibility
    // with cli tools or packages, e.g. knex cli (.ts)
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "declaration": false,
        "incremental": false,
        "sourceMap": false,
        "outDir": "/tmp/shits"
        // should never be compiled anyway
    }
}
