// import env from './env';
// import Debug from 'debug';

// const debug = Debug('config');

// class Config {
//     private history: Record<string, string>;

//     constructor(onInconsistency: (key: string, value: string) => void) {
//         this.history = {};
//     }

//     get(keys: string[]) {}
// }

// export default Config;
